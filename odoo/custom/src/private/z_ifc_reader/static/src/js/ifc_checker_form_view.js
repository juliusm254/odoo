odoo.define('z_ifc_reader.ifc_checker_form_view', function (require) {
    'use strict';

    var core = require('web.core');

    $(document).ready(function () {
        // Remove o_cp_buttons class to disable Save and Discard buttons
        $('.o_cp_buttons').removeClass('o_cp_buttons');
    });
});
